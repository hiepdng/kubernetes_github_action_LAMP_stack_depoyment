The below is the information used to build the DHI LAMP Project


#-----------------------------------------------------------------
Httpd Guides:
https://hub.docker.com/hardened-images/catalog/dhi/httpd/guides
https://hub.docker.com/_/httpd
https://httpd.apache.org/docs/2.4/ssl/ssl_faq.html

Image info.:
-Default:
  Entrypoint: httpd-foreground
  CMD: -
  User; 65532
  Working directory: /usr/local/apache2
-Environment variables
  HTTPD_PREFIX=/usr/local/apache2
  HTTPD_VERSION=2.4.68
  LD_LIBRARY_PATH=/usr/local/apr/lib:/usr/local/apr-util/lib
  PATH=/usr/local/apache2/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
  SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
Ports: 8080/tcp
       443/tcp
       
#-----------------------------------------------------------------

Mysql Guides: 
  https://hub.docker.com/hardened-images/catalog/dhi/mysql/guides
$ mysql --help
$ mysqld --help --verbose
$ mysqladmin variables -uroot -p
  Will display all default settings

Image info.:
-Configuration file locations:
   /etc/my.cnf
   /etc/mysql/my.cnf
   /out/opt/mysql/etc/my.cnf
   ~/.my.cnf 

-Environment variables
   DEBIAN_FRONTEND=noninteractive
   MYSQLDATA=/var/lib/mysql
   PATH=/opt/mysql/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

Ports:
   3306/tcp
   33060/tcp

 Entrypoint=/usr/local/bin/docker-entrypoint.sh
 Working directory=/var/www/html
 CMD=mysqld
 User=65532
 dates release: "2026-04-21"
 VERSION: 9.7.1

-Debug:
$ docker run --rm -it dhi.io/mysql:lts-debian13 bash

#-----------------------------------------------------------------

PHP-FPM Guides: 
  https://hub.docker.com/hardened-images/catalog/dhi/php/guides

$ php --ini
  Configuration File (php.ini) Path: "/opt/php-8.5/etc/php"
  Loaded Configuration File:         "/opt/php-8.5/etc/php/php.ini"
  Scan for additional .ini files in: "/opt/php-8.5/etc/php/conf.d"
  Additional .ini files parsed:      (none)

Image info.:
-Configuration file locations:
  /usr/local/etc/php/php.ini
  /opt/php-8.5/etc/php/php.ini

-Environment variables
  PATH=/opt/php-8.5/bin:/opt/php-8.5/sbin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
  PHP_INI_DIR=/usr/local/etc/php
  PHP_PREFIX=/opt/php-8.5
  PHP_SRC_DIR=/opt/php-8.5/src
  PHP_VERSION=8.5.8
  Entrypoint=/opt/php-8.5/sbin/php-fpm

Working directory=/var/www/html
User=65532

-Debug:
$ docker run --rm -it --entrypoint /bin/bash dhi.io/php:8.5.8-debian13-dev

#-----------------------------------------------------------------

HTTPD CA Certificates inside container:

mkdir -p /usr/local/share/ca-certificates/
openssl genrsa -out my-internal-ca.key 2048
openssl req -x509 -new -nodes -key my-internal-ca.key -sha256 -days 1826 -out /usr/local/share/ca-certificates/my-internal-ca.crt  -subj "/C=US/ST=state/L=City/O=Organization/OU=Department/CN=server.example.com/emailAddress=you@example.com"

# Update the certificate store to include your custom CA
apt-get update && \
apt-get install -y ca-certificates && \
update-ca-certificates

Behind the scenes on Debian, /etc/ssl/certs/ca-certificates.crt is generated by the update-ca-certificates utility.
That utility reads certificate files from /usr/share/ca-certificates/ and /usr/local/share/ca-certificates/, creates 
individually hashed symlinks in /etc/ssl/certs/, and then concatenates them all into the single ca-certificates.crt file
for applications that require a single bundle


#-----------------------------------------------------------------
HTTPD SSL/TLS Certificate:

# Create a self-signed SSL Certificate for testing purposes:
openssl genrsa -des3 -passout pass:YourPasswordHere -out certs/httpd/server.key.secure
openssl rsa -in certs/httpd/server.key.secure -passin pass:YourPasswordHere -out certs/httpd/server.key # decrypted server.key, used for auto start web withour password
openssl req -new -x509 -nodes -sha1 -days 365 -key certs/httpd/server.key -out certs/httpd/server.crt -extensions usr_cert  -subj "/C=US/ST=state/L=City/O=Organization/OU=Department/CN=server.example.com/emailAddress=you@example.com"

-Required enabled entries in httpd.conf:
Include conf/extra/httpd-ssl.conf
LoadModule ssl_module modules/mod_ssl.so
LoadModule socache_shmcb_module modules/mod_socache_shmcb.so

-Required enabled entries in httpd-ssl.conf
SSLEngine on
SSLCertificateFile /usr/local/apache2/conf/certs/server.crt
SSLCertificateKeyFile /usr/local/apache2/conf/certs/server.key


#-----------------------------------------------------------------
Apache server and php-fpm server connection

-Required enabled entries in httpd.conf:
-----
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_fcgi_module modules/mod_proxy_fcgi.so
<IfModule dir_module> 
    DirectoryIndex index.php index.html
</IfModule>
-----

-Required enabled entries in httpd-ssl.conf
-----
<VirtualHost _default_:443>
  ProxyPassMatch "^/(.*\.php(/.*)?)$" "fcgi://lamp-php-fpm:9000/var/www/html/$1" enablereuse=on
</VirtualHost>

<VirtualHost *:8080>
  ProxyPassMatch "^/(.*\.php(/.*)?)$" "fcgi://lamp-php-fpm:9000/var/www/html/$1" enablereuse=on
</VirtualHost>
-----

Where: lamp-php-fpm is the the php-fpm container name
        /var/www/html is the document path to read *.php file of the php-fpm container







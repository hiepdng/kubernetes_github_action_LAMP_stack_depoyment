[client]
ssl-mode=REQUIRED
ssl-ca=/opt/mysql/certs/ca.pem
ssl-cert=/opt/mysql/certs/client-cert.pem
ssl-key=/opt/mysql/certs/client-key.pem

[mysqld]
ssl-ca=/opt/mysql/certs/ca.pem
ssl-cert=/opt/mysql/certs/server-cert.pem
ssl-key=/opt/mysql/certs/server-key.pem
bind-address = *
require_secure_transport = ON

max_connections         = 150
max_connect_errors      = 100
connect_timeout         = 10
wait_timeout            = 28800
interactive_timeout     = 28800
back_log                = 10000

innodb_buffer_pool_size = 134217728
innodb_file_per_table   = ON
innodb_flush_log_at_trx_commit = 1
innodb_flush_method     = O_DIRECT

log_error               = stderr
slow_query_log          = OFF
long_query_time         = 10.000000

#This is for loading file into table
local-infile=1
